export const handleExportAttendance = (attendances, users, month) => {
    console.log('Exporting attendance for', month, attendances.length, 'records')
    // Placeholder function for excel export to prevent syntax errors
    // Should be implemented correctly later with xlsx library
    alert('Chức năng xuất Excel đang được cập nhật')
}
